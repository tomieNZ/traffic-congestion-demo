# Copy these values into your shell environment before starting the demo.
# The defaults in app.py are intentionally convenient for local evaluation only.
JWT_SECRET=replace-with-a-long-random-jwt-secret
HMAC_SECRET=replace-with-a-long-random-sensor-secret
DEMO_USERNAME=operator
DEMO_PASSWORD=demo-password
DATABASE_PATH=traffic_demo.sqlite3
RATE_LIMIT_PER_MINUTE=30
PORT=5000

